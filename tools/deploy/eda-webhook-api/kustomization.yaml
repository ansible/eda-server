apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
- pairs:
    comp: webhook-api
  includeSelectors: true

resources:
- deployment.yaml
- service.yaml

images:
- name: aap-eda
  newName: localhost/aap-eda
  newTag: latest
